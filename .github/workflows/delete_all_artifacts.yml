name: 'nightly artifacts cleanup'
on:
  workflow_dispatch:

jobs:
  delete-artifacts:
    runs-on: ubuntu-latest
    steps:
      - uses: kolpav/purge-artifacts-action@f7d2508
        with:
          token: $
          expire-in: 7days # Set this to 0 to delete all artifacts
