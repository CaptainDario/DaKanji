name: dakanji_db
description: Scripts to populate DaKanji's database
repository: https://github.com/captaindario/dakanji-db
version: 4.0.0

environment:
  sdk: ^3.5.4

# Add regular dependencies here.
dependencies:
  path: ^1.8.0
  drift: ^2.21.0
  sqlite3: ^2.4.6

dev_dependencies:
  drift_dev: ^2.21.0
  build_runner: ^2.4.13

  lints: ^4.0.0
  test: ^1.24.0
