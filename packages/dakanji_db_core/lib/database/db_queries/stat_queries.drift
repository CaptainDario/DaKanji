

get_mb_sizes_drift:
    SELECT
        name,
        sum(pgsize) AS bytes,
        format('%.2f MB', cast(sum(pgsize) AS REAL) / (1024 * 1024)) AS megabytes
    FROM dbstat
    GROUP BY name
    ORDER BY bytes DESC;
