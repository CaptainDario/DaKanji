name: sqlite_extensions
description: SQLite extensions bundled using native assets
version: 1.0.0
resolution: workspace

environment:
  sdk: '>=3.10.0'

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  sqlite3: ^3.1.6
  universal_io: ^2.2.2
  hooks: ^1.0.1
  code_assets: ^1.0.0
  da_db_shared:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
